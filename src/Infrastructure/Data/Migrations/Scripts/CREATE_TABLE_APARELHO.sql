CREATE TABLE APARELHO (
    ID SERIAL PRIMARY KEY,
    DESCRICAO VARCHAR(100) NOT NULL,
    TENSAO FLOAT NOT NULL,
    ID_REDE_ELETRICA INT NOT NULL
);

ALTER TABLE APARELHO ADD FOREIGN KEY (ID_REDE_ELETRICA) REFERENCES REDE_ELETRICA (ID);